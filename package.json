{
  "name": "content-detect",
  "version": "0.3.1",
  "private": true,
  "description": "A privacy-conscious browser extension for checking content against your account direction with TypeSafe Jev.",
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node scripts/browser-test.mjs",
    "audit:privacy": "node scripts/privacy-audit.mjs",
    "check": "npm test && npm run build && npm run audit:privacy"
  },
  "devDependencies": { "@playwright/test": "1.63.0", "esbuild": "0.25.12" }
}
