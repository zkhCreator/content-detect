# 验证记录

验证日期：2026-09-20。

0.3.2 页面调整：首页突出四要素，结果按内容价值、AI/情绪、辅助账号匹配排序；构建与浏览器完整回归通过，已查看首页、设置和结果截图。账号匹配依据改为可展开显示，服务请求及评分规则未调整。

## 已执行

- Node.js 22.22.2；`npm test`：30 项通过（含 0.3.0 四要素评分及不确定边界）。
- `npm run build`：生成独立 `dist/chrome`，无运行时 npm 依赖、远程脚本或 source map。
- Chromium 153.0.8010.12（Playwright 1.63.0）：加载实际 Manifest V3 包，使用隔离临时 profile 和合成网页完成回归。
- 本地截图已查看：初始入口、已保存设定、适合转发、建议复核、AI 倾向与负面情绪、不确定与混合情绪；0.3.0 另检查四要素正常评分、低置信度和选区节奏受限截图。未发现横向溢出，长内容和设定页允许纵向滚动。本轮浏览器回归包含 13 次被拦截的合成请求。
- `npm run audit:privacy`：扫描当前文件、打包目录、可达 Git 历史与提交邮箱，未发现所覆盖规则的隐私泄露。

## 覆盖范围

| 范围 | 验证内容 |
| --- | --- |
| Jev 契约 | 固定 endpoint、Bearer 头、单次请求中的 5 个 Noul、3 个 Choice、4 个 Score、结构化状态；严格验证概率、类别、置信度、分布总和和 Score 加权均值 |
| AI 与情绪 | 疑似 AI/偏向人工/不确定、全部情绪类别、混合情绪、低置信度、短文、上下文不足；新增分析不自动否决转发 |
| 内容价值 | 四项五档标准、0–4 加权评分、异常响应拒绝、低置信度待复核、上下文不足不评分；选区/截断/回退不评全文节奏；幽默分低不自动否决转发 |
| 输入与决策 | 空输入、方向上限、非法 Key、概率区间、冲突不可被平均抵消、内容不足/截断时复核 |
| 错误 | 401/403、429、5xx、4xx、网络失败、非 JSON、缺失答案、超时、取消 |
| 状态 | 并发去重、设置变更取消、旧响应不能恢复清除的数据、URL 变更丢弃旧结果 |
| 实际扩展 | 工具栏动作授予 activeTab；保存/清除配置；适合/复核/不建议 UI；AI/情绪与展开的概率分布；四要素评分、展开标准、低置信度和选区状态；旧缓存提示重新检查；HTTP/缺失 Choice 或 Score 错误；弹窗页面关闭后恢复 |
| 提取 | 正文、选区、长文截断、整页回退、空页；排除导航、隐藏节点、表单、编辑区与脚本文本 |
| 隐私 | 页面脚本读不到 local/session 存储；页面来源消息无法冒充弹窗；无 sync 存储；会话无正文、Key 或 URL 查询参数 |

## 明确未验证的事项

- **真实 Jev 模型调用与效果**：没有使用用户 Key，浏览器网络请求均被拦截为合成响应。验证的是官方契约、发送内容和应用行为，不是模型对真实账号内容的准确性、可用额度或线上延迟。AI 来源、情绪分类与四要素评分尚未做真实语料效果评估，不能据此声称 AI 检测可靠或评分已校准。
- **原生工具栏弹窗外壳**：该 macOS 测试浏览器在 CDP 自动附加原生 popup target 时崩溃。回归在真实工具栏授权后，将未经修改的打包 popup 页面打开为后台标签，继续使用实际后台 worker、存储、权限和网络调用。测试仅在临时 profile 内清空 action 的 popup 配置以绕开崩溃，未修改发布包的 manifest、代码或权限。原生尺寸约束、焦点切换和点击外部自动收起仍建议安装后人工确认。
- **Chrome 120、Edge、Firefox、Safari**：未取得对应运行时验证。120 是声明的最低 Chrome 版本；当前回归使用上面的 Chromium 版本。非 Chromium 版本只有架构计划。
- **所有站点和语言**：未承诺对所有动态网站、iframe、Shadow DOM、图片、视频、PDF 或任意长文章提取完整。中文效果需要用自有样本验证。
- **远端 CI 与仓库可见性**：源码已通过 SSH 推送到配置的远端，GitHub workflow 已配置；匿名 GitHub API 返回 404，无法据此读取远端 Actions 状态或确认可见性。本项目完成的是可公开检查，未更改仓库可见性。远端执行状态应以仓库 Actions 为准。

## 使用真实 Key 的人工验收

在本机加载扩展，使用自己的 Key 检查一篇允许发给 TypeSafe 的公开文章；分别用明显相关和明显无关的账号方向比较结果。再测试选中文字、修改 Key、关闭弹窗后重开、导航后重新检查，以及在浏览器设置页打开插件。不要将 Key 或私人内容写回仓库或验收截图。

使用 `npm run test:browser` 重建的截图及 JSON 摘要在被忽略的 `artifacts/` 目录，仅含测试合成内容。
