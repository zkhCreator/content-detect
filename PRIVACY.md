# 隐私说明

本扩展的单一用途是：在用户明确点击检查后，根据其账号方向判断当前页面文字的转发适配程度。

## 保存什么

| 数据 | 保存位置 | 保存时长 | 删除方式 |
| --- | --- | --- | --- |
| 用户自己填写的 Jev API key、账号方向 | 扩展 `storage.local` | 到用户清除或卸载扩展 | 「账号设定 → 清除 Key、方向与本次结果」 |
| 最近一次检查的概率、结论、时间、页面标题、来源站点、字符数、提取范围、标签 ID、URL 的 SHA-256 摘要 | 扩展 `storage.session` | 当前浏览器会话；后续检查覆盖 | 清除设定、标签导航、关闭目标标签或结束浏览器会话 |
| 正文或选中文字 | 当前检查的内存与 HTTPS 请求 | 本次检查期间 | 请求结束后不再由应用保留；不写入扩展存储 |

Key 不会被重新返回给弹窗输入框，仅后台读取并用于认证。`storage.local` 没有额外加密，不是系统钥匙串；浏览器配置所有者或本机管理员仍可能访问。该方案是用户自带 Key 的个人浏览器扩展，不应把开发者公用 Key 打包进去。未来若改成公共 Web 应用，应改用服务器端凭据。

## 发给谁

仅向 `https://api.typesafe.ai/v1/systemone` 发送：

- `Authorization: Bearer …` 中的用户 Key。
- 用户账号方向。
- 截断后的正文或选中文字、页面标题、来源 origin（协议、主机、端口）和提取范围。
- 五项固定问题与其判断标准。

请求不携带 Cookie、引用页面地址，也不跟随重定向。没有第三方分析、遥测、自建数据收集服务、自动后台扫描或自动重试。外部官网链接只有用户点击时才会打开。

页面标题、可见正文和账号方向本身也可能包含隐私；本扩展不是敏感信息识别/脱敏产品。不要检查未获准传输的内容。TypeSafe 对已收到请求的处理由其 [官方隐私政策及相关条款](https://docs.typesafe.ai/legal) 决定；清除本地数据不会删除第三方已经收到的请求，不宣称第三方零留存。

## 权限

- `activeTab`：用户从工具栏调用扩展后，临时获得当前标签的读取权限。
- `scripting`：用户点击检查后，在隔离环境提取当前页文字。
- `storage`：本地保存配置，并在会话内恢复结果。
- `https://api.typesafe.ai/*`：仅允许访问 Jev 接口所在域名。

没有 `<all_urls>`、`tabs`、`history`、`cookies`、`clipboardRead` 权限，没有永久内容脚本或对网页开放的资源。存储访问被限制在可信扩展上下文。网页文字作为不可信资料传给模型；即便模型判断被诱导，也不会执行页面或模型指令，更不会代用户发布内容。

## 仓库公开规则

不提交真实 Key、个人账号定位、真实页面抓取、用户数据、浏览器配置、环境文件或带私人信息的日志/截图。测试使用合成样本，构建产物和本地测试资料默认忽略。示例凭据明确标为测试数据。提交作者和提交者使用 GitHub noreply 邮箱。

如果真实密钥意外进入 Git，应先撤销密钥，再处理历史；只从当前文件删除并不能消除历史泄露。项目的隐私扫描覆盖当前文件及可达历史，但仍需人工检查新资料。
